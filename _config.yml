timezone: Asia/Shanghai
plugins:
  - jekyll-readme-index

readme_index:
  with_frontmatter: true

defaults:
  -
    scope:
      path: "README.*"
    values:
      layout: "pages"
  -
    scope:
      path: "archive/README.*"
    values:
      layout: "files"
